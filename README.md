# SwitchBotMeter_BLE_Decoder
This python3 script can be run e.g. on a RPI3 and will use bluetooth to scan for beacons from SwitchBot Meter/MeterPlus devices, decode them and publish the read values (battery, temperature, humidty) via MQTT.
